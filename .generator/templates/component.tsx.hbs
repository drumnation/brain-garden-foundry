import { Card, Text, Button, Stack, Group, Badge } from '@bg-kit/design-system';
import { spacing, colors } from '@bg-kit/design-system/tokens';
import { type ReactNode } from 'react';

export interface {{pascalCase name}}Props {
  /** Primary content or title */
  title: string;

  /** Optional description text */
  description?: string;

  /** Color variant for the component */
  variant?: 'primary' | 'success' | 'warning' | 'error' | 'info';

  /** Optional action button label */
  actionLabel?: string;

  /** Click handler for action button */
  onAction?: () => void;

  /** Optional icon element */
  icon?: ReactNode;
}

/**
 * {{pascalCase name}} Component
 *
 * A themable {{lowerCase category}} component built with the Brain Garden design system.
 * Supports dark/light mode and multiple color variants.
 *
 * @example
 * ```tsx
 * <{{pascalCase name}}
 *   title="Example Title"
 *   description="Example description"
 *   variant="primary"
 *   actionLabel="Click Me"
 *   onAction={() => console.log('clicked')}
 * />
 * ```
 *
 * @category {{category}}
 * @generator v1.0.0
 */
export function {{pascalCase name}}({
  title,
  description,
  variant = 'primary',
  actionLabel,
  onAction,
  icon,
}: {{pascalCase name}}Props) {
  return (
    <Card padding="lg" radius="md" withBorder>
      <Stack gap={spacing.md}>
        {icon && (
          <div>
            {icon}
          </div>
        )}

        <div>
          <Text size="lg" fw={600} mb="xs">
            {title}
          </Text>
          {description && (
            <Text size="sm" c="dimmed">
              {description}
            </Text>
          )}
        </div>

        {actionLabel && onAction && (
          <Group justify="flex-start" mt="md">
            <Button variant="light" color={variant} onClick={onAction}>
              {actionLabel}
            </Button>
          </Group>
        )}
      </Stack>
    </Card>
  );
}
